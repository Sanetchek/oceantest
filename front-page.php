<?php get_header(); ?>
<main class="wrapper home_page"><span class="opacity-bg"></span>

    <section class="container ">
        <a href="#slider-popup" class="open-popup-link">Open Popup</a>

        <div id="slider-popup" class="slider-popup mfp-hide">
            <h2 class="popup-title">Popup Content</h2>
            <div class="popup-content">This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. This is a simple popup. Click anywhere else on the page to close it. </div>
        </div>
    </section>

</main>
<?php get_footer(); ?>
